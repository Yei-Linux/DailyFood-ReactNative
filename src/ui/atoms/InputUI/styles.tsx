import {StyleSheet} from 'react-native';

export const InputStyles = StyleSheet.create({
  input: {
    paddingHorizontal: 5,
    paddingVertical: 5,
    borderRadius: 10,
    borderColor: '#cecccc',
    borderWidth: 1,
  },
});
