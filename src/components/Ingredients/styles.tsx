import {StyleSheet} from 'react-native';

export const IngredientsStyles = StyleSheet.create({
  ingredients: {
    overflow: 'hidden',
  },
  ingredientsSlider: {
    flexDirection: 'row',
    flexWrap: 'nowrap',
  },
});
